services:
  - type: web
    name: errata-report
    env: node
    plan: starter
    buildCommand: npm install
    startCommand: npm start
    disk:
      name: errata-report-data
      mountPath: /data
      sizeGB: 1
    envVars:
      - key: DB_PATH
        value: /data/data.json
